const friendsName = [ 'Indrajit', 'Mohon','Joudip','Nayem','Dipto'];
const evenName = [];
for(names of friendsName){
    // 
    if(names.length % 2==0){
        evenName.push(names);
    }
    else{
        false
    }
} 
console.log(evenName)